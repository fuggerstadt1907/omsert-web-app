import React from 'react';

const map = () => {

    const mapStyles = {
        width: '100%',
        height: '50vh',
        margin: 'auto',
        backgroundColor: 'lightgrey'
    };

    return (
        <div id="map" style={mapStyles} />
    );
}

export default map;